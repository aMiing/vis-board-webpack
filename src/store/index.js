import Vue from "vue";
import Vuex from "vuex";
import panel from './panel'

Vue.use(Vuex);

export default new Vuex.Store({
    state: {},
    getters: {},
    actions: {},
    modules: {
        panel
    },
});
